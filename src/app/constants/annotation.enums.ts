export enum EditState{
    continuous = 'continuous',
    single = 'single',
    update = 'update'
}

export enum EventState{
    active = 'active',
    complete = 'complete'
}

export enum EditEvents{
    create = 'create',
    delete = 'delete',
    redo ='redo',
    undo = 'undo',
    update='update'
}
